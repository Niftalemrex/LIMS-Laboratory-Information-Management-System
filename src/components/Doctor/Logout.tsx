import React from 'react';

const Logout: React.FC = () => (
  <section>
    <h1>Logout</h1>
    <p>Logging out...</p>
    {/* Logic for actual logout can go here */}
  </section>
);

export default Logout;
