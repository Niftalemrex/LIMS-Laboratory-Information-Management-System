import React from 'react';

const Logout: React.FC = () => {
  return (
    <div>
      <h2>Logout</h2>
      <p>You have successfully logged out.</p>
      {/* Add your logout logic (redirect, token clear) here if needed */}
    </div>
  );
};

export default Logout;
